import re

def preprocess_text(text: str) -> str:
    # Basic cleanup for an MVP
    text = text.replace("\r", "\n")
    text = re.sub(r"\n{3,}", "\n\n", text)
    text = re.sub(r"[ \t]+", " ", text)
    return text.strip()